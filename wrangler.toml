name = "bright-gift"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# This file tells Cloudflare Pages this is an Astro SSR build
# The actual build output directory is configured in Cloudflare Pages dashboard as "dist"

